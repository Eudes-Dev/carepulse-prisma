'use client'
//WIP: ERROR PAGE

import React from 'react'

export default function GlobalError({ error }: { error: Error & { digest?: string }}) {
    React.useEffect(() => {
        
    })
  return (
    <div>GlobalError</div>
  )
}